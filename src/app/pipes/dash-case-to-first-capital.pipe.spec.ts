import { DashCaseToFirstCapitalPipe } from './dash-case-to-first-capital.pipe';

describe('DashCaseToFirstCapitalPipe', () => {
  it('create an instance', () => {
    const pipe = new DashCaseToFirstCapitalPipe();
    expect(pipe).toBeTruthy();
  });
});
